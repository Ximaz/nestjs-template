{
  "name": "project",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "pnpm": {
    "overrides": {
      "fastify": "^5.8.5"
    },
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "@scarf/scarf",
      "@swc/core",
      "argon2",
      "esbuild"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "test": "pnpm --filter=./packages/backend test",
    "lint": "pnpm --filter=./packages/backend lint",
    "format": "pnpm --filter=./packages/backend format"
  }
}
